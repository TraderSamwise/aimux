# Rust Orphan Sweep v1

Generated by:

```bash
node scripts/audit-rust-orphans.mjs
```

Method: parse Rust function definitions under `native/crates/aimux/src` and
`native/crates/aimux/tests`, start production reachability at bin `main`
functions, and report public functions that are not production-reachable and
have no production reference to their symbol outside their own definition. The
scanner is conservative: same-named calls mark all same-named functions
reachable, so false negatives are more likely than false positives.

Raw result from this tree:

- Public functions checked: 1668
- Graph-unreachable public functions: 490
- Unreferenced production public functions: 310
- Actionable after fixture/contract exclusions: 116
- Test-only reachable: 74
- TUI/expose-owned: 11
- High-signal no-reference candidates needing Node-caller triage: 31

The full current table is the script output. This document records the initial
manual triage for the 31 high-signal candidates.

| Rust symbol | Initial triage |
| --- | --- |
| `backend_session_ids::discover_backend_session_id` | Delete candidate. Node called `discoverBackendSessionId`, but Rust production callers use `discover_backend_session_id_with_options` directly. Behavior appears wired through the options-aware path. |
| `CoreCommandCall::transport_request` | Delete candidate. Convenience wrapper around `build_core_command_transport_request`; no production caller. |
| `daemon_supervisor::stop_project_service` | Delete candidate. Node exported `stopProjectService`; Rust routes `/projects/stop` through daemon/core-command paths directly. |
| `daemon_supervisor::project_service_status` | Delete candidate. Node exported `projectServiceStatus`; Rust status reads go through daemon routes directly. |
| `daemon::listener::serve_daemon_http` | Delete candidate. Production uses `serve_daemon_http_with_metadata_and_interceptor`; this wrapper is not used. |
| `DashboardProjectRefreshState::observe_all` | Delete candidate. Batch helper; production uses `observe`/`observe_for_screen`. |
| `PathResolver::hosted_audit_prompts_path` | Delete candidate unless hosted audit prompts are revived. Node had the path helper, but no production caller was found. |
| `PathResolver::hosted_devices_path` | Delete candidate unless hosted device storage is revived. Node had the path helper, but no production caller was found. |
| `PathResolver::project_team_path_for` | Delete candidate. Project team config is wired through `project_service::team::project_team_path`. |
| `PathResolver::remove_project` | Delete candidate unless project deregistration is reintroduced. Node exported `removeProject`, but no production caller was found. |
| `paths::require_git_project_root` | Delete candidate after the non-git reversal. Keeping it callable risks reintroducing the rejected non-git refusal. |
| `paths::is_ephemeral_temp_project_root` | Delete candidate. The `_from` implementation is the wired path used by registration normalization. |
| `process_inspector::is_native_aimux_project_service_process` | Delete candidate. Runtime coherence uses the current-binary verifier; this older wrapper has no production caller. |
| `project_service_manifest::compute_current_project_service_manifest` | Likely missing wiring. Node exposed and used the current-manifest computation in runtime guard drift checks. Rust uses `get_project_service_manifest`, but this exact self-drift path needs review before deletion. |
| `project_service_manifest::has_project_service_build_drift` | Likely missing wiring. Node production called `hasProjectServiceBuildDrift` from runtime guard; Rust has no production caller. |
| `ProjectServiceRoutePattern::exact_path` | Delete candidate. `display_path` and `matches` are wired; this accessor is not. |
| `ProjectServiceRouteSpec::exact_binary` | Delete candidate unless a binary exact route is added. No route spec uses it. |
| `ProjectServiceRouteSpec::prefix_binary` | Delete candidate unless a binary prefix route is added. No route spec uses it. |
| `project_service::routes::canonical_project_api_routes` | Delete candidate or contract-only helper. The canonical route list is fixture-tested but not a production route dependency. |
| `project_service::runtime_events::route_runtime_set_attention` | Delete candidate. Production metadata routing calls `route_runtime_set_attention_with_context`; this context-free wrapper is unused. |
| `ProjectHotSnapshotCoordinator::with_refresh_delay_ms` | Delete candidate. Test/configuration seam with no production caller. |
| `terminal_key_parser::is_shifted_letter_command` | Likely missing wiring in TUI key handling. Node production called this from dashboard control, dashboard interaction, and navigation. Route to codex-u1iogs before editing. |
| `TmuxSessionTransport::set_backend_session_id` | Likely missing wiring or stale API. Node session runtime wrote `backendSessionId` through the transport; Rust has no production caller. |
| `TmuxSessionTransport::manager_mut` | Delete candidate. Mutable test seam/accessor; no Node production equivalent found. |
| `TmuxSessionTransport::retarget` | Likely missing wiring. Node production retargeted tmux-backed transports during persistence and session runtime reconciliation. Rust has no production caller. |
| `TmuxSessionTransport::destroy` | Needs review. Node session runtime called `destroy`; Rust method is a no-op and has no production caller. |
| `TmuxRuntimeManager::with_session_prefix` | Delete candidate unless private tmux prefixes are still required outside tests. No production caller. |
| `TmuxRuntimeManager::peek_open_session_name` | Delete candidate. Node exposed `peekOpenSessionName`; Rust appears to use other open-session helpers. |
| `TmuxRuntimeManager::ensure_project_session_async` | Needs review. Node production called the async form from session launch; Rust may have intentionally collapsed onto the sync path, but that should be proven before deletion. |
| `TmuxRuntimeManager::has_window` | Likely missing wiring. Node used this from service resurrection and runtime restart guards. |
| `TmuxRuntimeManager::link_window_to_session` | Likely missing wiring. Node runtime restart linked dashboard windows into client sessions; Rust has the ported method but no production caller. |

The likely missing-wiring set is therefore:

- `project_service_manifest::compute_current_project_service_manifest`
- `project_service_manifest::has_project_service_build_drift`
- `terminal_key_parser::is_shifted_letter_command`
- `TmuxSessionTransport::set_backend_session_id`
- `TmuxSessionTransport::retarget`
- `TmuxSessionTransport::destroy`
- `TmuxRuntimeManager::ensure_project_session_async`
- `TmuxRuntimeManager::has_window`
- `TmuxRuntimeManager::link_window_to_session`

Do not mass-delete the delete candidates from this report without a second
pass. The point of the sweep is to expose unwired ports before deleting or
rewiring anything.
