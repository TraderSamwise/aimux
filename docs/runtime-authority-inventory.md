# Runtime Authority Inventory

This inventory is the working map for the runtime-core hard cut. Its job is to identify, for each domain, what currently writes state, what currently reads state, what should become authoritative, and which legacy paths must be removed or demoted to projections.

Use this file together with [runtime-core-hard-cut-roadmap.md](runtime-core-hard-cut-roadmap.md). The roadmap describes the long-haul execution order; this inventory tracks the concrete authority map that makes those phases auditable.

## Status Labels

- `CUT`: the domain already has the intended source of truth.
- `PARTIAL`: some write/read paths use the intended source, but parallel authority remains.
- `LEGACY`: the domain still uses old authority and needs a hard cut.
- `PROJECTION`: the domain should not be authoritative; it should be rebuilt from another source.
- `UNKNOWN`: the domain still needs source inspection before planning a cut.

## Authority Types

- `runtime topology`: `runtime-topology.yaml` through `src/runtime-core/*`.
- `runtime exchange`: `runtime-exchange.yaml`, the future authoritative exchange model for messages, handoffs, tasks, reviews, waits, and inbox state. It must not duplicate `.aimux/threads` / `.aimux/tasks` authority. Topology may reference exchange records through `exchangeRefs`, but must not own exchange state directly.
- `project service memory`: daemon-supervised project service state that should be treated as a runtime cache unless explicitly persisted through an authoritative model.
- `tmux`: live terminal substrate and binding evidence, not durable coordination truth.
- `projection`: derived state such as metadata, statusline, notifications, debug views, and GUI caches.
- `legacy file`: current JSON/thread/task files that may need migration, export-only status, or removal.

## Domain Matrix

| Domain | Status | Current Authority | Target Authority | Current Readers | Current Writers | Keep As Projection/Cache | Remove Or Demote |
| --- | --- | --- | --- | --- | --- | --- | --- |
| Agent lifecycle | PARTIAL | `runtime-topology.yaml` for offline/graveyard state, live tmux metadata for running windows, `offlineSessions` as an in-memory cache | runtime topology | `src/runtime-core/topology-sessions.ts`, `src/multiplexer/runtime-state.ts`, `src/multiplexer/session-launch.ts`, `src/multiplexer/session-runtime-core.ts`, `src/main.ts`, `src/project-scanner.ts`, teammate lifecycle routes | `upsertTopologySession`, `saveRuntimeTopologySessions`, `moveTopologySessionToGraveyard`, `resurrectTopologySession`, `/agents/resume`, `/agents/teammates/resume`, plus cache writes to `offlineSessions` | `offlineSessions` may remain only as a rebuilt runtime/UI cache; tmux remains live substrate evidence | `offlineSessions` as authority, lifecycle writes hidden behind `saveState`, tmux metadata as durable lifecycle truth, teammate lifecycle routes outside topology |
| Agent tmux bindings | PARTIAL | tmux window metadata plus `sessionTmuxTargets`; topology carries durable session identity but not yet all binding recovery semantics | runtime topology binding records plus tmux evidence | `src/multiplexer/runtime-state.ts`, `src/multiplexer/session-runtime-core.ts`, `src/tmux/runtime-manager.ts`, `scripts/tmux-control.sh`, statusline builders | `createSession`, `restoreTmuxSessionsFromTopology`, `syncTmuxWindowMetadata`, `setWindowMetadata`, `repairManagedTmuxTargets` | `sessionTmuxTargets` as process-local handle cache; tmux metadata as live verification | durable binding decisions inferred only from tmux window metadata or window names |
| Agent graveyard | PARTIAL | topology session status through `moveTopologySessionToGraveyard`; public resurrection paths still fail closed pending runtime-core replacement; old in-process `graveyardEntries` remains declared but should not decide truth | runtime topology session status | `src/main.ts`, `src/runtime-core/topology-sessions.ts`, `src/multiplexer/runtime-state.ts`, debug/project scan paths, graveyard dashboard/API/CLI paths | `moveTopologySessionToGraveyard`; `resurrectTopologySession` exists as a helper but is not fully wired through public resurrection paths | dashboard/debug lists may project topology graveyard records | `graveyardEntries`, fail-closed public resurrection, and any non-topology graveyard JSON/state resurrection path |
| Team relationships | PARTIAL | project/global `team.json` role config plus session `team` metadata persisted on topology session records and mirrored in tmux metadata/UI caches | runtime topology nodes/edges or session team metadata plus explicit role config authority | `src/team.ts`, `src/task-workflow.ts`, `src/multiplexer/session-runtime-core.ts`, dashboard/statusline composition, runtime topology session readers, tmux metadata repair paths | `saveTeamConfig`, `saveGlobalTeamConfig`, session creation/fork/restore paths and topology session upserts | dashboard teammate grouping, role preambles, review-routing projection, statusline teammate projection | team membership or review routing inferred from UI caches, tmux metadata, or unclassified `team.json` authority |
| Services | LEGACY | `.aimux/state.json` `services`, live tmux service metadata, and `offlineServices` process cache | runtime topology service records | `src/multiplexer/runtime-state.ts`, `src/multiplexer/services.ts`, `src/multiplexer/service-state-snapshot.ts`, `src/multiplexer/persistence-methods.ts`, metadata-server/CLI service endpoints | `createService`, `stopService`, `resumeOfflineService`, `removeOfflineService`, `persistProjectRuntimeSnapshotsBeforeTmuxStop`, `host.saveState` | `offlineServices` can remain a rebuilt UI cache; tmux metadata can remain live service substrate evidence | `.aimux/state.json` services as authority, direct JSON edits in service removal, service lifecycle hidden behind `saveState` |
| Worktrees | LEGACY | `git worktree list`, dashboard pending-action caches, and operation-failure JSON | runtime topology worktree records | `src/worktree.ts`, `src/multiplexer/worktrees.ts`, `src/multiplexer/persistence-methods.ts`, app/API worktree routes | `createDesktopWorktree`, `removeDesktopWorktree`, dashboard worktree mutation helpers, git commands | git remains substrate evidence; dashboard pending actions remain transient projection | dashboard caches or operation failures acting as durable worktree existence/status truth |
| Worktree graveyard | LEGACY | `.aimux/worktree-graveyard.json`; runtime methods currently throw replacement-required errors for graveyard mutate operations | runtime topology worktree graveyard records | `src/multiplexer/worktree-graveyard.ts`, `src/multiplexer/persistence-methods.ts`, service/session availability filters | existing readers only; mutation methods are intentionally blocked pending runtime-core replacement | graveyard list can be projected from topology for GUI/debug | `worktree-graveyard.json` as authority and any resurrection/delete path that bypasses topology |
| Direct messages | LEGACY | `.aimux/threads/*.json` thread records plus `.jsonl` message logs; routing rules in `src/orchestration-routing.ts` | runtime exchange | `src/threads.ts`, `src/orchestration.ts`, `src/orchestration-routing.ts`, `src/multiplexer/agent-io-methods.ts`, `src/metadata-server.ts`, `src/main.ts`, `app/lib/api.ts` | `createThread`, `appendMessage`, `updateMessage`, `markMessageDelivered`, `sendThreadMessage`, `sendDirectMessage`, `/threads/open`, `/threads/send` | HTTP/API DTOs, GUI thread summaries, delivery notifications, routing previews | thread JSON/JSONL as authority, message delivery state stored only in thread logs, routing semantics outside exchange |
| Handoffs | LEGACY | handoff threads in `.aimux/threads` with `kind: "handoff"` and message metadata; dashboard has an unbacked `/tasks/handoff` client submission path | runtime exchange | `src/orchestration-actions.ts`, `src/metadata-server.ts`, dashboard route picker, CLI handoff commands, dashboard `/tasks/handoff` client path | `sendHandoff`, `acceptHandoff`, `completeHandoff`, `/handoff`, `/handoff/accept`, `/handoff/complete`, legacy `/threads/send` handoff mode | handoff UI and alert projections | handoff lifecycle represented only as thread status/message conventions, divergent or unbacked handoff submission routes |
| Plans | LEGACY | `.aimux/plans/*.md` files written by project-service plan routes and session bootstrap | runtime exchange or explicitly separate durable plan authority | `src/metadata-server.ts`, `src/session-bootstrap.ts`, `src/builtin-metadata-watchers.ts`, `app/lib/api.ts`, plans screens | `/plans/*` project-service routes, session bootstrap plan creation, app plan save APIs | plan editor/viewer UI, metadata progress projection | plan markdown as hidden coordination authority without an explicit owner model |
| Continuity context/history | LEGACY | `.aimux/history/*.jsonl`, `.aimux/context/*`, and `.aimux/recordings/*` continuity/backfill files | runtime exchange or explicitly separate durable continuity authority | `src/context/history.ts`, `src/context/compactor.ts`, `src/context/context-file.ts`, `src/context/context-bridge.ts`, `src/recorder.ts`, `src/session-bootstrap.ts`, `src/multiplexer/session-launch.ts`, metadata watchers | `appendTurn`, `Recorder`, context compactor/bridge writers, session bootstrap fork/restore copy paths | transcript/continuity projections, raw recordings, and compaction artifacts | history/context/recording files injected into agents or restore/fork flows without an explicit authority boundary |
| Status files | LEGACY | `.aimux/status/*.md` session status files consumed by bootstrap and metadata watchers | projection or exchange-owned agent status note authority | `src/session-bootstrap.ts`, `src/multiplexer/session-runtime-core.ts`, `src/builtin-metadata-watchers.ts` | agents/status writers, fork/restore copy paths, status cleanup paths | metadata headline/progress projection | status markdown as hidden coordination or standing-context authority |
| Attachments | LEGACY | `.aimux/attachments/*.json` metadata plus payload files | runtime exchange payload store or explicitly separate blob authority | `src/attachment-store.ts`, `src/metadata-server.ts`, app message rendering, relay sharing allowlist, `/attachments/*` project-service read routes | no in-source writer currently found; existing project-service attachment routes are read-only | public attachment DTOs and content URLs | attachment metadata as untracked message payload authority or relay-visible payload scope outside exchange; any future ingest/write path outside exchange/blob authority |
| Tasks | LEGACY | `.aimux/tasks/*.json`, with linked thread IDs in `.aimux/threads`; topology now only carries `exchangeRefs` for cross-model references | runtime exchange | `src/tasks.ts`, `src/orchestration-actions.ts`, `src/task-workflow.ts`, `src/workflow.ts`, `src/builtin-metadata-watchers.ts`, metadata-server/CLI/app workflow surfaces | `writeTask`, `assignTask`, `acceptTask`, `blockTask`, `completeTask`, `reopenTask`, `TaskWorkflow` review creation, `/agents/teammates/tasks`, `/agents/teammates/create` `initialTask` | task cards, workflow entries, metadata watcher summaries | task JSON as authority, task side effects hidden in metadata watchers, role-only assignment detached from topology identity, teammate route task creation outside exchange, any topology-owned exchange queue/state |
| Reviews | LEGACY | review tasks in `.aimux/tasks/*.json` plus review threads | runtime exchange | `src/tasks.ts`, `src/task-workflow.ts`, `src/orchestration-actions.ts`, dashboard review request flow, CLI/app review routes | `requestReview`, `approveReview`, `requestTaskChanges`, automatic review/revision task creation | review UI/status projections and follow-up alerts | review lifecycle encoded as task subtype/status instead of first-class exchange records |
| Waiting/inbox state | LEGACY | thread `status`, `owner`, `waitingOn`, `unreadBy`; task `status`; workflow rollups; notification unread counts; derived session semantics | runtime exchange | `src/workflow.ts`, `src/session-semantics.ts`, `src/threads.ts`, `src/tasks.ts`, `src/notifications.ts`, dashboard/statusline/app screens | `setThreadStatus`, `markThreadSeen`, message/task/handoff/review mutations, notification read/clear mutations | workflow entries, per-agent inbox badges, statusline attention scores, notification unread UX | waiting/inbox state scattered across workflow, threads, tasks, notifications, and metadata-derived fields |
| Metadata | PROJECTION | global project `metadata.json` via `src/metadata-store.ts`; `AgentTracker` activity/attention/event state via metadata-server endpoints; used by dashboard/statusline/API readers as decoration only | projection | `src/metadata-store.ts`, `src/metadata-server.ts`, `src/agent-tracker.ts`, `src/multiplexer/persistence-methods.ts`, app API consumers | `updateSessionMetadata`, `saveMetadataState`, `/event`, `/mark-seen`, `/set-activity`, `/set-attention`, `/shell-state`, clear-log/transcript helpers, metadata API endpoints | progress, logs, transcript paths, plugin statusline segments, derived activity/attention, endpoint discovery | metadata or tracker state deciding lifecycle, ownership, labels, worktree existence, exchange waits, or backend identity |
| Notification records | CUT | tagged runtime exchange threads/messages/inbox entries | runtime exchange | notification list/count endpoints, dashboard/app notification consumers, status summaries, project event/notify paths | `addNotification`, `upsertNotification`, `markNotificationsRead`, `clearNotifications`, dashboard failure publishing | notification list DTOs and badges | notification records/read/clear state outside exchange |
| Notification context/views | PROJECTION | global project `notification-context.json` suppresses/focuses notification display | projection over exchange/activity/lifecycle | `src/notification-context.ts`, dashboard/app notification consumers, status summaries | `updateNotificationContext`, `/notification-context` | focus/suppression context, notification panels, unread projections | notification context as the only durable record of task state, waits, handoffs, lifecycle, attention, or failures that belong in topology/exchange |
| Statusline | PROJECTION | global project `statusline.json` and precomputed tmux statusline text files; retired local `.aimux/statusline.json` is ignored | projection over topology/exchange/metadata | `src/tmux/statusline.ts`, `scripts/tmux-statusline.sh`, tmux prefix/statusline integrations, dashboard client statusline | `writeStatuslineFile`, `buildStatuslineSnapshot`, `writePrecomputedTmuxStatuslineFiles`, `refreshProjectStatusline` | statusline JSON/text files, tmux-render caches | statusline snapshots as lifecycle/service/task authority |
| Fast-control / tmux prefix navigation | PARTIAL | tmux-local metadata/statusline/last-used files with project-service `/control/*` fallback | tmux-local bindings plus project-service fallback | `scripts/tmux-control.sh`, `src/fast-control.ts`, `src/tmux/runtime-manager.ts`, `src/tmux/switcher.ts`, metadata-server control routes | tmux binding installer, `markLastUsed`, project-service control endpoints | switch menus, recency cache, statusline attention projection | project-service or metadata files becoming the only source for latency-sensitive local navigation |
| GUI project snapshot | PROJECTION | daemon `/projects`, project-service `/desktop`, `/worktrees`, `/graveyard`, `/threads`, `/workflow`, app Jotai stores | projection over project-service authoritative reads | `app/lib/api.ts`, `app/lib/heartbeat.ts`, `app/stores/projects.ts`, app screens, dashboard HTTP clients | project-service snapshot builders, daemon project listing, SSE alerts/output streams, app local preference stores | client caches, optimistic pending actions, transient chat/output state | GUI stores deciding runtime lifecycle/exchange truth or retaining divergent durable copies |
| CLI commands | PARTIAL | mixed local file reads, topology reads, and daemon/project-service HTTP calls | clients over authoritative APIs/models | `src/main.ts` command handlers, daemon client helpers, topology/session/task/thread readers | CLI lifecycle commands, thread/task/review commands via project-service, some local fallback mutations | CLI output formatting and JSON DTOs | CLI commands mutating legacy files or caches directly after authoritative APIs exist |
| Project service HTTP/SSE | PARTIAL | per-project service process over runtime caches, legacy files, metadata, notifications, topology reads, tmux substrate | API layer over topology/exchange/projections | `src/metadata-server.ts`, `app/lib/api.ts`, `app/lib/heartbeat.ts`, dashboard-control helpers, relay proxy | metadata-server route handlers, lifecycle adapter methods, `resolveAlertRecipients`, event bus/SSE emitters | API DTOs, SSE alerts/output, alert recipient projection, compatibility route wrappers | route handlers that directly own persistence or inbox routing instead of delegating to topology/exchange/projection stores |
| Daemon project registry | CUT | global daemon state plus desktop project discovery and per-project metadata endpoint files | daemon registry | `src/daemon.ts`, `src/daemon-state.ts`, `src/project-scanner.ts`, app project store, CLI daemon commands | `ensureProject`, `stopProject`, daemon state save/load, metadata endpoint save/remove | service endpoint discovery, process supervision status | daemon registry carrying per-agent lifecycle/exchange truth |
| Remote clients and ownership | PARTIAL | relay transport/share/security state (`security-state:v1`, `sharing-state:v1`), app settings `ownerUserId`/`shareId`, daemon relay status, legacy `instances.json` presence | runtime topology ownership/presence plus relay-owned transport/security authority | `app/lib/relay-transport.ts`, relay API helpers, `relay/src/security.ts`, `relay/src/sharing.ts`, `relay/src/relay-object.ts`, `src/daemon.ts`, `src/instance-registry.ts`, app settings/security stores | relay connect/request flows, `/security/*`, `/shares/*`, `loadSecurityState`/`saveSecurityState`, `loadSharingState`/`saveSharingState`, daemon relay enable/disable, instance heartbeat files | remote transport/session security events, share participation UI, presence display, relay security/share policy | relay/share/instance files deciding topology ownership, agent lifecycle, or cross-agent work authority |
| Debug state | PROJECTION | live reads across topology, metadata, tmux, graveyard, worktree graveyard, notifications, dashboard snapshot | projection/audit view | `src/debug-state.ts`, debug CLI/API consumers | debug-state assembly only | complete audit/report surface | debug output or snapshots used as writable recovery state |
| Migration/compatibility state | LEGACY | compatibility readers/writers for in-repo `.aimux`, legacy JSON files, and old API routes | one-way import/export tooling | `src/instance-registry.ts`, metadata-server removed-route shims, project scanner, debug-state | dual writes for `instances.json`, legacy route responses, future importers | explicit migration/export commands and read-only diagnostics | silent dual authority, compatibility writes without an owner-removal date |

## Audit Columns

Each domain row should eventually answer:

- `Current Authority`: the file, in-memory object, tmux metadata, API surface, or external service that currently decides truth.
- `Target Authority`: the single intended source after the hard cut.
- `Current Readers`: code paths that read the domain, including GUI and CLI.
- `Current Writers`: code paths that mutate the domain.
- `Keep As Projection/Cache`: files or state that can remain if clearly non-authoritative.
- `Remove Or Demote`: code paths, files, endpoints, or docs that must not remain parallel truth.

## Dead-Path Checklist

The concrete checklist lives in [runtime-authority-dead-paths.md](runtime-authority-dead-paths.md). This matrix should reference that checklist rather than carrying every `rg` pattern inline.
