# Claude Code

@AGENTS.md

## Claude-Specific Notes

- Use `/memory` to inspect which Claude memory files are loaded.
- Claude settings and permissions live in `.claude/settings.json` or `.claude/settings.local.json`, not in this file.
